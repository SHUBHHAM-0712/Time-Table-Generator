[pytest]
addopts = -ra
testpaths = tests
markers =
    performance: performance-sensitive tests
    e2e: browser (Playwright) UI tests; needs: pip install -r requirements-test.txt && playwright install chromium
